name: yvr-conf

url:
  - yvr-conf.paas.allizom.org

framework:
  runtime: node
  type: node

disk: 2048
mem: 64

ignores: ["node_modules", "bower_components", "styles", "app"]

env:
  NODE_ENV:
    default: production
    required: y
    inherit: y
